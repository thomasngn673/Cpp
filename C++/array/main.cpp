#include <iostream>
using namespace std;

int main() {
    int array[5] = {99,98,97,96,95};
    // array can hold as many integers as in the number in the brackets
    cout << array[3] << endl;
    // computers start counting at 0, not 1
    // 99 is index 0, 98 is index 1, 97 is index 2
}
